
<div class="ui container">
    <div class"ui grid">
      <h1>Pick your location</h1>
      <%= link_to "New location", new_trip_location_path(@trip) %>

      <div class "sixteen wide computer column">
        <div class="ui cards">
          <% @trip.locations.each do |local| %>


          <div class="card">
            <div class="content">
              <div class="header">
                <%= local.name %>
              </div>

              <div class="description">
                <%= local.country %>
              </div>
              <div class="description">
                <%= local.address %>
              </div>
              <div class="description">
                <%= local.priority %>
              </div>
            </div>
            <div class="extra content">
              Add option to add to trip
            </div>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
