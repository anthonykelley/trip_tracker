

      <div class "sixteen wide computer column">
        <div class="ui cards">
          <% @trip.locations.each do |local| %>


          <div class="card">
            <div class="content">
              <div class="header">
                <%=link_to local.name, location_path(local) %>
              </div>

              <div class="description">
                <%= local.country %>
              </div>
              <div class="description">
                <%= local.address %>
              </div>
              <div class="description">
                <strong>Description: </strong><%= local.description.truncate(65) %>
              </div>
            </div>
            <div class="extra content">
              <%= link_to 'Remove This location', remove_location_path(@trip, local), method: :patch, class: "ui basic orange button" %>
            </div>
          </div>
          <% end %>
        </div>
      </div>
